<Window x:Class="ConferenceManager.WPF.Views.SpeakerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConferenceManager.WPF.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{Binding DialogTitle}"
        Height="600" Width="500"
        WindowStartupLocation="CenterOwner"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{materialDesign:MaterialDesignFont}"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Name:" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,5"/>
                <TextBox x:Name="NameTextBox" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="Enter name"
                         Margin="0,0,0,10"/>

                <TextBlock Text="Email:" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,5"/>
                <TextBox x:Name="EmailTextBox" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="Enter email"
                         Margin="0,0,0,10"/>

                <TextBlock Text="Phone:" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,5"/>
                <TextBox x:Name="PhoneTextBox" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="Enter phone"
                         Margin="0,0,0,10"/>

                <TextBlock Text="Organization:" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,5"/>
                <TextBox x:Name="OrganizationTextBox" Text="{Binding Organization, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="Enter organization"
                         Margin="0,0,0,10"/>

                <TextBlock Text="Biography:" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,5"/>
                <TextBox x:Name="BiographyTextBox" Text="{Binding Biography, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="Enter biography"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Height="100"
                         Margin="0,0,0,10"/>

                <TextBlock Text="Expertise:" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,5"/>
                <TextBox x:Name="ExpertiseTextBox" Text="{Binding Expertise, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="Enter expertise"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Height="100"
                         Margin="0,0,0,10"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" 
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,10,0"/>
            <Button Content="Save" 
                    Command="{Binding SaveCommand}"
                    IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window> 
<UserControl x:Class="ConferenceManager.WPF.Views.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ConferenceManager.WPF.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Exportation des données" 
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   Margin="16"/>

        <StackPanel Grid.Row="1" Margin="16">
            <TextBlock Text="Choisissez le format d'exportation :"
                       Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                       Margin="0,0,0,16"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,32">
                <Button Content="Excel"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,16,0"
                        Click="ExportToExcel_Click"/>
                <Button Content="PDF"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="ExportToPdf_Click"/>
            </StackPanel>

            <TextBlock Text="Données disponibles pour l'exportation :"
                       Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                       Margin="0,0,0,16"/>

            <StackPanel Margin="16,0,0,0">
                <TextBlock Text="• Conférences (titre, date, lieu, description, statut)"/>
                <TextBlock Text="• Conférenciers (nom, email, téléphone, biographie, statut)"/>
                <TextBlock Text="• Documents (titre, type, date, statut)"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl> 